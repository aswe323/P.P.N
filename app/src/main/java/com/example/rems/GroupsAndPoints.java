package com.example.rems;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

/*
 *
 * join group will open a dialo box asking the name\ID of the group
 *
 * the create a group will be delete if i already have a group.
 * join a group won't be visible if im in a group
 *
 * */
public class GroupsAndPoints extends AppCompatActivity {

    /*@Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_groups_and_points);
    }*/
}
