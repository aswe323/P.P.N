public enum Repetition {
    no_repeating,
    every_24_hours,
    every_month,
    every_year,
    every_week,
    every_sunday,
    every_monday,
    every_tuesday,
    every_wednesday,
    every_thursday,
    every_friday,
    every_saturday
    //TODO: disable auto duplication of classes to java father directory;
}
